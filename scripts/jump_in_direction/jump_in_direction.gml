function jump_in_direction(distance, direction){
	/// @description jump_in_direction(distance, direction)
	/// @param distance
	/// @param direction
 
	// Teleports parent object to a spot based on given direction and distance
	x += lengthdir_x(distance,direction)
	y += lengthdir_y(distance,direction)
}